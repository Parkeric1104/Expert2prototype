# PRD — SVC-004 운영 백오피스(BO)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (초안) |
| 작성일 | 2026-08-11 |
| 대상 시스템 | 세법/노무 AI 도우미 — 운영 백오피스 (신규) |
| 관련 문서 | [BO_DESIGN_REFERENCE.md](BO_DESIGN_REFERENCE.md) (디자인 기준), 서비스 요구사항 SVC-001~003 |
| 범위 제외 | **계정관리 없음** — 운영자 계정 CRUD·RBAC·권한등급을 만들지 않는다 (§3.2) |

---

## 1. 배경·문제

서비스 화면에 노출되는 운영 콘텐츠 — **공지사항(SVC-001)·공지팝업(SVC-002)·버전정보(SVC-003)** — 가 현재 프론트엔드 정적 소스(`src/app/data/service-content.ts`)에 하드코딩되어 있다. 공지 한 줄을 바꾸려 해도 **코드 수정 → 빌드 → 배포**가 필요하며, 운영 담당자가 개발자를 거치지 않고는 아무것도 바꿀 수 없다.

## 2. 목적

운영 담당자가 **배포 없이** 공지사항·공지팝업·버전정보를 직접 등록/수정/노출제어하고, 변경이 서비스에 **즉시(허용 지연 내) 반영**되는 관리자 웹 콘솔을 제공한다.

## 3. 범위

### 3.1 포함 (In Scope)
- 공지사항 CRUD + 노출 제어 + 미리보기
- 공지팝업 관리(활성/심각도/문구) + 미리보기
- 버전정보 관리(서비스 버전, 법령 DB 갱신일)
- 공지 조회수 집계·표시
- 변경 이력(라이트 감사로그)
- FE 데이터 공급 API (기존 정적 소스 대체)

### 3.2 제외 (Out of Scope) — 확정
- **계정관리 전부**: 운영자 계정 생성/삭제, 로그인 화면, RBAC/권한등급, 승인 워크플로. 접근 통제는 **네트워크 레벨(사내망/IP 제한 등) 전제**로 하며 BO 자체는 인증 기능을 갖지 않는다.
  - 파생 효과: 변경 이력에 "누가"는 기록 불가 → 시각·변경내용만 기록(§7.5). 계정 개념 도입 시 확장.
- 카테고리(공지 유형) 관리 화면 — 유형은 코드 고정(공지/릴리즈노트), BO는 선택만.
- 서비스 FE 화면 자체의 변경 (이미 반영 완료).
- 지표 대시보드(차트) — 후순위(M3, §12).

## 4. 사용자·시나리오

**사용자 = 서비스 운영 담당자(비개발자).** 대표 시나리오:
1. 정기 점검 안내 → 공지 작성(유형=공지, 중요 체크) → 미리보기 확인 → 게시 → 서비스에 즉시 노출, 사이드패널 종에 레드닷.
2. 장애 발생 → 공지팝업 활성화(심각도=긴급, 메시지 입력) → 메인 중앙 모달로 즉시 노출 → 상황 종료 후 비활성화.
3. 릴리즈 → 버전정보의 서비스 버전 갱신 + 릴리즈노트 유형 공지 등록.
4. 법령 DB 갱신 → 법령 DB 갱신일 수정 → 푸터·법령 카테고리 하단에 반영.

## 5. 정보 구조 (IA)

콘솔 셸(좌측 사이드바 + 콘텐츠 카드, [디자인 참조 §1](BO_DESIGN_REFERENCE.md)):

```
BO 콘솔
├─ 공지사항        (목록 / 작성·수정)
├─ 공지팝업        (단일 설정 화면)
├─ 버전정보        (단일 설정 화면)
└─ 변경 이력       (읽기 전용 목록)
```

## 6. 화면 설계 방향

룩앤필은 **apps in toss 콘솔 스타일**([BO_DESIGN_REFERENCE.md](BO_DESIGN_REFERENCE.md))을 따른다. 화면별 차용 패턴:

| 화면 | 구성 |
|---|---|
| 공지 목록 | 흰 카드 테이블: 우상단 `공지 등록` Primary 버튼, 카드 헤더에 [유형 필터][검색], 컬럼 = 작성일·유형·제목·중요·조회수·상태(dot)·액션. 행 우측 칩 액션(수정/삭제). 페이지네이션 우하단 |
| 공지 작성/수정 | 섹션 카드 폼 + **우측 스티키 미리보기**(서비스 공지 상세 모습 실시간 렌더). 필수=라벨 빨간 dot. 필드: 유형(세그먼트), 제목(60자 카운터), 본문(프리텍스트), 게시일, 중요(토글), 노출기간(선택) |
| 공지팝업 | 상단 **"현재 노출 상태" 요약 카드**(활성 여부 + 심각도 dot + 제목). 아래 설정 카드: 활성(토글), 심각도(세그먼트: 안내/주의/긴급 — 아이콘·색 동일 규칙), 제목, 메시지(150자 카운터). **우측 스티키 미리보기**(실제 중앙 모달 모습). 긴급 선택 시 옐로 주의 배너("모든 사용자에게 즉시 노출됩니다") |
| 버전정보 | 요약 카드(현재 서비스 버전 크게 + 법령 DB 갱신일) + 수정 폼(버전 문자열, 날짜 피커) |
| 변경 이력 | 읽기 전용 테이블: 시각·대상(공지/팝업/버전)·동작(생성/수정/삭제/활성화…)·변경 요약 |

## 7. 기능 요구사항

### 7.1 공지사항 관리
| ID | 요구사항 | 우선순위 |
|---|---|---|
| BO-N-01 | 공지 목록 조회: 최신순, 유형 필터(전체/공지/릴리즈노트), 제목 검색, 페이지네이션 | M1 |
| BO-N-02 | 공지 등록: 유형·제목(≤60자)·본문(프리텍스트)·게시일·중요 여부 입력, 필수값 검증 | M1 |
| BO-N-03 | 공지 수정·삭제 (삭제는 확인 모달 필수) | M1 |
| BO-N-04 | 미리보기: 저장 전 서비스 실제 렌더 형태로 확인(우측 스티키 패널) | M1 |
| BO-N-05 | 노출기간(시작~종료) 설정 시 기간 외 자동 비노출, 미설정 시 상시 노출 | M2 |
| BO-N-06 | 예약 발행: 게시일이 미래면 해당 시점부터 노출 | M2 |

### 7.2 공지팝업 관리
| ID | 요구사항 | 우선순위 |
|---|---|---|
| BO-P-01 | 팝업 활성/비활성 토글 — 활성 시 서비스 메인 중앙 모달로 노출 | M1 |
| BO-P-02 | 심각도 3단계(안내/주의/긴급) 선택 — 서비스와 동일한 아이콘·색 매핑 | M1 |
| BO-P-03 | 제목·메시지(≤150자) 입력 + 실시간 미리보기 | M1 |
| BO-P-04 | 현재 노출 상태 요약 카드(활성 여부·심각도·제목 한눈에) | M1 |
| BO-P-05 | 노출기간 설정(선택): 종료 시각 도달 시 자동 비활성 | M2 |

단일 팝업만 관리(동시 1건). '다시 보지 않기' 억제는 FE 로컬 플래그(기구현) — BO에서 팝업 내용을 **수정 후 재활성화하면 새 노출로 간주**되어 억제가 풀려야 함(팝업 id/리비전 갱신).

### 7.3 버전정보 관리
| ID | 요구사항 | 우선순위 |
|---|---|---|
| BO-V-01 | 서비스 버전 문자열 수정 (예: v1.4.0) | M1 |
| BO-V-02 | 법령 DB 갱신일 수정 (날짜 피커) | M1 |

### 7.4 조회수
| ID | 요구사항 | 우선순위 |
|---|---|---|
| BO-C-01 | 공지 상세 열람 시 조회수 카운트(서비스 → API) | M2 |
| BO-C-02 | 중복 방지: distinct 기준은 오픈 이슈(§11) — 초안은 "브라우저 로컬 기준 공지당 1회" | M2 |
| BO-C-03 | 목록에 조회수 표시 | M2 |

### 7.5 변경 이력 (라이트)
| ID | 요구사항 | 우선순위 |
|---|---|---|
| BO-L-01 | 모든 쓰기 동작을 이력으로 기록: 시각·대상·동작·변경 전/후 요약 | M2 |
| BO-L-02 | 이력은 읽기 전용, BO 내 목록 화면 제공 | M2 |

계정이 없으므로 행위자(누가)는 기록하지 않는다. 필요해지면 계정 도입과 함께 확장.

### 7.6 반영(무배포)
| ID | 요구사항 | 우선순위 |
|---|---|---|
| BO-S-01 | BO 저장 → 서비스 반영 지연 ≤ 1분(캐시 TTL 또는 무효화) | M1 |
| BO-S-02 | 긴급(팝업 활성/비활성)은 즉시 반영 경로 우선 검토 | M1 |

## 8. 데이터 계약·API 초안

FE의 `getNotices / getEmergency / getServiceVersion`(현 `service-content.ts`)을 API fetch로 대체한다.

```
GET  /api/content/notices        → Notice[]      (서비스 소비)
GET  /api/content/popup          → Popup | null  (active 포함)
GET  /api/content/version        → { service, lawDataUpdatedAt }
POST /api/content/notices/{id}/view  → 조회수 +1 (M2)

BO 쓰기:
POST/PUT/DELETE /api/admin/notices[/{id}]
PUT  /api/admin/popup
PUT  /api/admin/version
GET  /api/admin/audit-log
```

엔티티 필드 (기존 FE 계약 유지):
- **Notice**: `id · type(공지|릴리즈노트) · title · body · date · important · views` (+ M2: `publishAt · expireAt`)
- **Popup**: `active · severity(info|warning|critical) · title · message` (+ `revision` — 재활성화 시 증가, '다시 보지 않기' 리셋 키)
- **Version**: `service · lawDataUpdatedAt`

## 9. 비기능 요구사항

- **접근 통제**: BO는 무인증. 사내망/IP 제한 등 네트워크 레벨 차단을 배포 전제 조건으로 명시.
- **PE(폐쇄망) 대응**: 근로복지공단 등 PE 환경은 외부 원격 config 접근 불가 → 동일 BO를 **내부망에 함께 구축**하거나 콘텐츠를 내부 소스(파일/내부 API)로 공급. PE 구축 시 BO 포함 여부는 PE PRD에서 결정.
- 한국어 UI, `wordBreak: keep-all`, 모든 요소 라운드 — 서비스와 공통 규칙 유지.
- 삭제 등 파괴적 동작은 확인 모달 필수.

## 10. 확정된 정책 (서비스 측 세션에서 결정, 재확인용)

1. 공지 카테고리(탭)는 코드 고정(전체/공지/릴리즈노트) — BO는 유형 선택만.
2. 심각도 3단계(안내/주의/긴급): 아이콘·색으로 구분, 라벨 태그 없음.
3. 길이 제한: 제목 ≈60자, 팝업 메시지 ≈150자, 공지 본문 프리텍스트.
4. 팝업 '다시 보지 않기' = FE 로컬 플래그 / 종 아이콘 레드닷 = 미열람 공지 존재 시.
5. 버전 표기: 푸터(서비스+법령DB), 법령 카테고리 하단(법령DB만).
6. **계정관리·RBAC 없음** (본 PRD에서 확정).

## 11. 오픈 이슈

| # | 이슈 | 초안 의견 |
|---|---|---|
| 1 | 조회수 distinct 기준 | 브라우저 로컬 기준 공지당 1회 (계정 없음 전제와 일관) |
| 2 | 공지 본문 서식 범위 | 줄바꿈 + 링크 자동 인식까지, 마크다운은 보류 |
| 3 | 캐시 반영 지연 허용치 | 일반 1분 / 팝업 즉시 |
| 4 | 팝업 노출 대상(전체/특정) | M1은 전체만, 대상 지정은 계정/식별 체계 없어 보류 |
| 5 | 변경 이력 보존기간 | 1년 초안 |
| 6 | PE 콘텐츠 공급 방식 | 내부망 BO 동반 구축 vs 정적 파일 반출 — PE PRD로 이관 |
| 7 | BO 접근 통제 방식(무인증 전제) | 사내망 제한으로 충분한지 보안 검토 필요 |

## 12. 마일스톤

- **M1 (MVP)**: 콘솔 셸 + 공지 CRUD·미리보기 + 팝업 관리 + 버전 관리 + FE API 전환 + 반영(캐시)
- **M2**: 노출기간·예약 발행 + 조회수 집계 + 변경 이력
- **M3 (후순위)**: 지표 대시보드(조회 추이 차트), 카테고리 관리
