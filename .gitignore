# 의존성/빌드 산출물
node_modules/
dist/

# 환경변수 — .env.example만 커밋, 실제 값 파일은 제외 (프록시 URL/키 유출 방지)
.env
.env.*
!.env.example

# 로컬 도구
.claude/worktrees/
.DS_Store

# 기획 정책 챗봇 로컬 DB (POC) — 데이터는 각자 로컬에만 보관
data/
