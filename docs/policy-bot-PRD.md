# PRD — 기획 정책 챗봇 (내부용) POC

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (초안) — 2026-07-27 |
| 작성 | Claude (박대웅 검토 전) |
| 상태 | **검토 대기** — §9 오픈 이슈 확정 후 v1.0 |
| 대상 시스템 | Expert2prototype 저장소 내 별도 페이지 `/policy-bot.html` |

## 1. 배경 / 문제

세법/노무 AI 도우미의 기획 정책(답변 유형, 멀티턴 횟수, 의견서 플로우, 피드백 게이팅 등)이
PRD·코드 주석·커밋 메시지·ECM(OneFFICE) 문서에 흩어져 있다. 팀원(PM·디자이너·개발자)이
"이 정책이 뭐였지?"를 확인하려면 사람에게 묻거나 코드를 뒤져야 한다.

→ **정책을 한 곳에 모으고, 질문하면 근거 문서와 함께 답해주는 내부 챗봇**을 만든다.

## 2. 목표 / 비목표

### 목표 (POC)
- G1. 팀원이 자연어로 기획 정책을 질문하면 **근거 문서 발췌 + 출처**로 답한다.
- G2. 정책 문서를 **로컬 DB에서 관리**한다 (커밋/재배포 없이 추가·수정).
- G3. 어떤 질문이 들어오는지 **대화 로그를 축적**한다 (문서화 우선순위 파악용).
- G4. 프로토타입 본체와 **주소·코드 경로를 분리**해 운영한다.

### 비목표 (POC에서 하지 않음)
- 정식 배포·인증(사내 SSO)·권한 관리
- 관리용 UI (문서 편집 화면) — *"UI는 나중에"* 확정
- ECM 자동 동기화 (인증 제약, §7)
- 답변 품질 고도화(임베딩 검색, 평가 루프)

## 3. 사용자 / 시나리오

- **사용자**: 세법/노무 도우미 팀 내부 구성원 (PM·디자이너·개발자). 엔드유저 아님.
- **S1**: 디자이너가 "플로팅 버튼 라벨 분기 기준?"을 질문 → 확정일·PRD ID 포함 답변.
- **S2**: 신규 합류자가 온보딩 중 정책을 챗봇으로 self-serve 학습.
- **S3**: PM이 대화 로그를 보고 "자주 묻는데 문서가 없는 정책"을 발견 → 문서 보강.

## 4. 기능 요구사항

상태: ✅ 구현됨 · 🔜 예정 · ⏸ 보류(결정 대기)

| ID | 요구사항 | 상태 |
|---|---|---|
| PB-001 | 별도 주소 진입: `/policy-bot.html` (Vite 멀티페이지, 프로토타입과 분리) | ✅ |
| PB-002 | 지식 베이스: 정책 문서를 `##` 섹션 단위로 청크 분할해 검색 | ✅ |
| PB-003 | 검색 답변(오프라인 모드): 관련 발췌 상위 3건 + 근거 문서 링크 제시 | ✅ |
| PB-004 | 근거 없는 질문엔 "문서에 없음"을 명시 (추측 금지) | ✅ |
| PB-005 | 로컬 DB 저장: SQLite(`data/policy-bot.db`) — 문서 CRUD API + 최초 자동 시드 | ✅ |
| PB-006 | 대화 로그 저장: 질문/답변/출처/모드 → `chat_logs` | ✅ |
| PB-007 | DB 미가용 시 번들 문서로 폴백 (정적 배포본에서도 동작) | ✅ |
| PB-008 | 임베드 위젯: 스크립트 한 줄로 타 페이지에 부착 (`policy-bot-widget.js`) | ✅ |
| PB-009 | LLM 답변(Claude): 검색 발췌를 근거로 요약 답변 생성 — 프록시 코드 준비됨, **엔드포인트/키 미확정** | ⏸ |
| PB-010 | 대화 로그 조회 화면(또는 간단 리포트) | 🔜 |
| PB-011 | 문서 관리 UI | ⏸ ("나중에") |
| PB-012 | ECM(OneFFICE) 동기화 | ⏸ (API/서비스 계정 확보 시) |

## 5. 데이터

로컬 SQLite (`server/policy-db.mjs`). 정식 전환 시 스키마 그대로 Postgres(Neon/Supabase) 이관.

```sql
policies  (id TEXT PK, title, source, content, updated_at)   -- 정책 문서 (md)
chat_logs (id PK, question, answer, sources JSON, offline, created_at)
```

- 시드: 최초 실행 시 `docs/policies/*.md` + ONBOARDING/Guidelines/DESIGN_SYSTEM 자동 적재.
- `data/`는 gitignore — POC 동안 데이터는 각자 로컬 보관.

## 6. 아키텍처

### 현재 (POC)
```
브라우저(/policy-bot.html)
  → GET /api/policy-bot/policies  (Vite 개발서버 미들웨어)
  → SQLite data/policy-bot.db
답변: 클라이언트 키워드 검색(발췌 제시) · 로그: POST /api/policy-bot/logs
```
- `npm run dev` 하나로 전부 동작. 프로덕션 정적 배포본은 번들 문서 폴백(PB-007).

### 정식 전환 경로 (참고)
```
저장소: Neon/Supabase(Postgres)  ·  API: Vercel proxy/  ·  LLM: Claude (proxy/api/policy-bot.js)
검색: pgvector 임베딩(문서 증가 시)  ·  진입: 위젯(PB-008) or 독립 도메인
```

## 7. 제약 / 리스크

| 항목 | 내용 | 대응 |
|---|---|---|
| ECM 인증 | 공유 토큰 링크가 그룹웨어 로그인으로 리다이렉트 (2026-07-27 확인) | 수동 내보내기 → `docs/policies/` 커밋 or DB API 등록. API 확보 시 PB-012 |
| LLM 미확정 | 사내 LLM 엔드포인트/Claude 키 미정 | 오프라인 모드로 POC 진행, 프록시는 `ANTHROPIC_BASE_URL`로 교체 가능하게 준비됨 |
| 공개 배포 | GitHub Pages는 공개 — 번들 문서도 열람 가능 | POC는 로컬 중심. 대외비 문서는 DB에만 넣고(번들 제외) 정식 전환 시 접근 제어 |
| 데이터 휘발 | 로컬 DB는 개인 머신에만 존재 | POC 한정 수용. 공유 필요해지면 §6 정식 전환 |

## 8. 마일스톤

| 단계 | 내용 | 상태 |
|---|---|---|
| M1 | 챗봇 화면 + 문서 검색 답변 + 별도 주소 | ✅ 완료 (2026-07-27) |
| M2 | 임베드 위젯 | ✅ 완료 |
| M3 | 로컬 DB(문서·로그) | ✅ 완료 |
| M4 | 대화 로그 조회(PB-010) + 정책 문서 보강 | 🔜 다음 |
| M5 | Claude 연동(PB-009) | 엔드포인트 확정 시 |
| M6 | 정식 전환(저장소·배포·접근 제어) | POC 검증 후 결정 |

## 9. 오픈 이슈 (검토·결정 필요)

- **Q1. 지식 범위**: 디자인 가이드/온보딩 문서도 KB에 포함 유지? 아니면 "기획 정책"만으로 한정?
- **Q2. 로그 활용**: PB-010을 화면으로 만들까, 주기적 텍스트 리포트면 충분할까? 로그에 질문자 식별이 필요한가(현재 익명)?
- **Q3. LLM**: 사내 LLM 엔드포인트 확인 결과에 따라 — Claude API 직접 결제 vs 사내 게이트웨이. POC 단계에서 먼저 Claude 무료/개인 키로 실험할지?
- **Q4. 문서 등록 절차**: POC 동안 ECM 문서를 누가·어떤 주기로 내보내 등록할지 (md 커밋 vs DB API).
- **Q5. "다시 해보자"의 범위**: 기존 구현 위에 PRD 기준으로 보완·정리(권장) vs 특정 부분 재설계?
