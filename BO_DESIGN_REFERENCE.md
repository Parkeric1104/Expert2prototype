# SVC-004 운영 백오피스(BO) — 디자인 참조 (apps in toss 콘솔)

> 참조 원본: 토스 **apps in toss 개발자 콘솔**(https://apps-in-toss.toss.im/workspace/…) 실사용 스크린샷 6장 (2026-08-11 사용자 제공: 대시보드 / 앱 출시 목록 / 버전 등록 모달 / 의견 보내기 팝오버 / 유저정보 연동 등록 폼).
> 용도: SVC-004 BO PRD·프로토타입의 룩앤필 기준. **BO는 서비스 FE의 하드룰(배경 그라데이션, Blue 500 #5784FF)과 별개의 어드민 룩앤필**로 간다 — 아래 토스 콘솔 스타일을 따른다.

## 1. 전체 레이아웃 (콘솔 셸)

- **2단 구조**: 좌측 고정 사이드바(약 216px, 흰색/아주 옅은 회색 배경) + 우측 콘텐츠 영역(옅은 회색 배경 `#F5F6F8`대, 그 위에 흰색 카드).
- 사이드바 구성(위→아래):
  1. 서비스 로고(워드마크)
  2. **워크스페이스/컨텍스트 스위처 카드**: 앱 이름 + 부제목 + 우측 chevron(>) — 회색 라운드 카드
  3. 보조 링크 필 버튼 2분할(`가이드 | 의견 보내기`)
  4. **그룹핑된 내비게이션**: 섹션 라벨(예: `개발`, `수익화`, `유입·성장`)은 소형 회색 텍스트, 항목은 [컬러 아이콘 + 라벨]. **선택 항목 = 옅은 회색 라운드 배경**(pill/rounded-lg). 호버도 동일 계열.
- 콘텐츠 상단: 페이지 타이틀(굵게, ~22–24px) + 바로 아래 한 줄 설명(회색, "~할 수 있어요" 톤) + **우상단 Primary 액션 버튼 1개**(예: `버전 등록`).
- 대시보드형 화면은 상단에 **탭(필 형태)** 이 별도로 뜸: 선택 탭 = 회색 필 배경, 신규 기능엔 `N` 레드 뱃지.
- 우하단 **플로팅 채널톡(문의) 버튼**(파란 원형 + 레드닷).

## 2. 컬러 톤

- **Primary 블루**: 버튼·링크·포커스에 일관 사용. 콘솔 버튼은 `#3B5BF6~#4B7BF5`대(토스 블루 계열, 채도 높은 인디고-블루). BO 프로토타입에서는 **Blue 500 `#5784FF`를 Primary로 그대로 써도 무방** (톤이 거의 같음).
- 배경: 콘텐츠 영역 옅은 회색, 카드 흰색 — **카드로 위계를 만들고 보더는 거의 안 씀**(그림자도 매우 약함, 카드 radius로 구분).
- 상태색: 성공/출시 = 초록 dot, 대기/검토 = 회색 dot, 경고 배너 = 옅은 노랑 배경 + 주황 `!` 아이콘, 알림 뱃지 = 레드.
- 차트: 블루(메인 라인, 선 아래 옅은 블루 그라데이션 fill), 보조 시리즈 = 초록·주황.

## 3. 타이포그래피·텍스트 톤

- 산세리프(토스체/Pretendard 계열). 페이지 타이틀 굵게, 본문 14–15px, 보조설명·컬럼헤더 12–13px 회색.
- **마이크로카피는 해요체**: "버전을 등록하고 실제 토스 앱에 출시할 수 있어요.", "어떤 번들인지 메모를 남겨보세요." — BO도 동일한 안내 톤 권장.
- 긴 텍스트는 말줄임 + `… 더보기` 인라인 확장.

## 4. 핵심 컴포넌트 패턴

### 목록/테이블 (→ BO 공지 목록에 적용)
- 흰색 라운드 카드 안에 테이블. 카드 헤더: 좌측 섹션 타이틀, **우측에 [상태 필터 드롭다운] [보조 버튼] [검색 인풋(돋보기+플레이스홀더)]** 순 정렬.
- 컬럼 헤더는 소형 회색 텍스트, 로우는 옅은 회색 라운드 로우(스트라이프처럼 보이는 부드러운 배경).
- **상태 = 컬러 dot + 텍스트** (초록 `현재 출시됨` / 회색 `검토 필요`) — 뱃지 박스보다 가벼운 표현.
- 인라인 편집: 메모 칸에 ✏️ 연필 아이콘(클릭 시 편집).
- **행 우측에 액션 칩 버튼**: 회색 칩(`테스트`) + 블루 틴트 칩(`검토 요청`, 옅은 블루 배경 + 블루 텍스트).
- 페이지네이션: 우하단, 현재 페이지 원형 + ‹ › 화살표(최소 형태).
- 목록 위에 **"현재 상태" 요약 카드**를 별도 배치(초록 dot + "현재 출시된 버전" + 큰 굵은 버전 문자열 + 회색 타임스탬프). → BO에서 "현재 노출 중인 팝업/현재 서비스 버전" 요약 카드로 그대로 응용.

### 모달 (→ 공지 작성/팝업 편집에 적용)
- 중앙 모달, 흰색 rounded-2xl, 딤 배경. 타이틀 좌상단 굵게.
- **필수 표시 = 라벨 우상단 빨간 dot**(별표 아님).
- 인풋: 라운드 보더, 포커스 시 블루 보더 + 옅은 블루 링. 파일 인풋은 📎 아이콘 + 플레이스홀더.
- 인풋 아래 회색 helper text 1줄. 관련 안내는 **회색 라운드 정보 패널**(제목+설명)로 삽입.
- 푸터: 우측 정렬 [닫기(회색 secondary)] [등록하기(블루 primary)] — 라벨은 동사형.

### 등록/설정 폼 페이지 (→ 팝업·버전 관리 화면에 적용)
- 상단 **브레드크럼**(`유저정보 불러오기 / 등록하기`, 현재 단계 굵게).
- 폼을 **섹션별 흰색 카드**로 분할(카드마다 섹션 타이틀 + 설명 1줄). 활성 편집 중인 그룹은 **블루 아웃라인 라운드 박스**로 강조.
- 반복 그룹은 `+ 노출 시점 추가하기` 형태의 **블루 텍스트 + ⊕ 아이콘 버튼**으로 추가.
- 다중 선택 = 칩(태그 ×) 인풋. on/off는 **토글 스위치 + OFF 라벨**. 라디오는 소형 원형.
- 주의사항은 **옐로 배너**(옅은 노랑 배경, 주황 ! 아이콘, 본문 1–2줄)로 섹션 상단에.
- **우측 스티키 라이브 프리뷰 패널**: 사용자가 실제로 보게 될 화면을 카드로 실시간 미리보기. → **BO 공지팝업/공지 작성 화면의 '미리보기' 요구사항을 이 패턴(우측 스티키 프리뷰)으로 구현**.

### 대시보드 (→ 조회수/지표 화면에 적용, 후순위)
- 기간 컨트롤: 세그먼트(일별/주별/월별) + 날짜 범위 피커, 우상단 고정.
- 차트 카드: 타이틀 + `?` 툴팁 아이콘, 우측에 세그먼트 토글 + `다운로드` 버튼.
- 도넛 차트: 중앙에 대표값(큰 % + 카운트), 우측 범례(컬러 dot·라벨·수·%). 카드 우상단에 `총 n명` 합계.

### 피드백 팝오버
- 소형 카드: × 닫기, 굵은 타이틀, 회색 부연, 텍스트에어리어, 우측 블루 버튼. (BO에도 운영자 피드백 채널로 응용 가능)

## 5. SVC-004 화면별 매핑 요약

| BO 화면 | 차용 패턴 |
|---|---|
| 공지 목록 | 테이블 카드(필터+검색+칩 액션) + 상태 dot + 페이지네이션 + `더보기` 말줄임 |
| 공지 작성/수정 | 모달(소규모) 또는 섹션 카드 폼(대규모) + 빨간 dot 필수표시 + 우측 스티키 미리보기 |
| 공지팝업 관리 | 섹션 카드 폼 + 토글(활성) + 라디오/세그먼트(심각도) + 옐로 주의 배너 + 우측 스티키 미리보기 + "현재 노출 중" 요약 카드 |
| 버전 관리 | 요약 카드(현재 버전 크게) + 이력 테이블 |
| 조회수/지표 | 대시보드 카드(기간 세그먼트 + 차트) — 후순위 |
| 콘솔 셸 | 좌측 사이드바(그룹 내비) + 페이지 타이틀/설명/우상단 Primary 버튼 |

## 6. 하드룰과의 관계

- 서비스 FE 하드룰(전체 배경 그라데이션, 페르소나 색 등)은 **BO에 적용하지 않는다**. BO는 위 어드민 룩앤필(옅은 회색 바탕 + 흰 카드)이 기준.
- 공통으로 유지: 모든 요소 라운드(rounded-none 금지), 한국어 `keep-all`, Primary 블루 계열(`#5784FF` 사용 가능).
